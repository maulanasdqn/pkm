import * as React from 'react';
import { ActivityModule } from './_modules';
import { NextPage } from 'next';

const ActivityPage: NextPage = (): React.ReactElement => {
  return <ActivityModule />;
};

export default ActivityPage;
