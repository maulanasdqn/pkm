import { FC, ReactElement } from 'react';
import { NewsDetailModule } from './_modules';

const NewsDetailPage: FC = (): ReactElement => {
  return <NewsDetailModule />;
};

export default NewsDetailPage;
