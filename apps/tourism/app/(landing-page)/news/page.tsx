import { NextPage } from 'next';
import { ReactElement } from 'react';
import { NewsPageModule } from './_modules';

const NewsPage: NextPage = (): ReactElement => {
  return <NewsPageModule />;
};

export default NewsPage;
