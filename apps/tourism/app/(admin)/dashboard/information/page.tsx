import { NextPage } from 'next';
import { ReactElement } from 'react';
import { InformationModule } from './_modules';

const DashboardInformationPage: NextPage = (): ReactElement => {
  return <InformationModule />;
};

export default DashboardInformationPage;
