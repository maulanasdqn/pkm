import { NextPage } from 'next';
import { ReactElement } from 'react';
import { TourModule } from './_modules';

const DashboardTourPage: NextPage = (): ReactElement => {
  return <TourModule />;
};

export default DashboardTourPage;
