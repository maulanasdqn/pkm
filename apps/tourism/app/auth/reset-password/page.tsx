import { NextPage } from 'next';
import { ReactElement } from 'react';
import { ResetPasswordModule } from './_modules';

const ResetPasswordPage: NextPage = (): ReactElement => {
  return <ResetPasswordModule />;
};

export default ResetPasswordPage;
