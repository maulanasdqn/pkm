import { NextPage } from 'next';
import { ReactElement } from 'react';
import { ProductIdModule } from './_modules';

const ProductIdPage: NextPage = (): ReactElement => {
  return <ProductIdModule />;
};

export default ProductIdPage;
