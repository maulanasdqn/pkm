import { NextPage } from 'next';
import { ReactElement } from 'react';
import { CartsModule } from './_modules';

const CartsPage: NextPage = (): ReactElement => {
  return <CartsModule />;
};

export default CartsPage;
