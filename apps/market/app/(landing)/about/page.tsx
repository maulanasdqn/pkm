import { NextPage } from 'next';
import { ReactElement } from 'react';
import { AboutModule } from './_modules';

const AboutPage: NextPage = (): ReactElement => {
  return <AboutModule />;
};

export default AboutPage;
