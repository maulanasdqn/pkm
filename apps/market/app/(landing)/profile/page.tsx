import { NextPage } from 'next';
import { ReactElement } from 'react';
import { ProfileModule } from './_modules';

const ProfilePage: NextPage = (): ReactElement => {
  return <ProfileModule />;
};

export default ProfilePage;
